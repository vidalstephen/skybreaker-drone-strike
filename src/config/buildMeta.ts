/**
 * Build metadata updated by the phase-worker after each completed phase.
 * Displayed in the bottom-right corner of the splash screen.
 */
export const PHASE_TAG = 'Phase 8b';

/**
 * When true, all prototype/PLANNED arc missions are immediately selectable
 * regardless of campaign progress. Flip to false before any public release.
 */
export const DEV_UNLOCK_PROTOTYPES = true;
